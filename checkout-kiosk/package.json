{
  "name": "checkout-kiosk",
  "version": "1.0.0",
  "description": "Electron kiosk app for self-checkout with USB NFC reader",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild",
    "postinstall": "electron-rebuild"
  },
  "dependencies": {
    "nfc-pcsc": "^0.8.1"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "@electron/rebuild": "^3.7.1"
  }
}
