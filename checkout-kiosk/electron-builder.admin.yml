# Copyright Offene Werkstatt Wädenswil
# SPDX-License-Identifier: MIT
#
# Builds the admin-mode binary. Mode is baked in by scripts/inject-mode.mjs
# before this config is read (npm run build:admin sets BRIDGE_MODE=admin).

appId: ch.werkstattwaedi.admin
productName: OWW Admin
directories:
  output: release/admin
files:
  - dist/**/*
  - renderer/**/*
  - package.json
  - node_modules/**/*
extraMetadata:
  name: oww-admin
win:
  target:
    - target: nsis
      arch: x64
  artifactName: oww-admin-${version}-setup.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
linux:
  target:
    - AppImage
  artifactName: oww-admin-${version}-${arch}.${ext}
mac:
  target: dmg
  artifactName: oww-admin-${version}-${arch}.${ext}
