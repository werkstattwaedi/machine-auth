# Your Particle product ID or slug (visible in Particle Console)
# Find it at: https://console.particle.io/
PARTICLE_PRODUCT_ID=""

# Particle access token for API authentication
# Create one with: particle token create
PARTICLE_TOKEN=""

# Your Firebase project ID (same as gcloud project ID)
# Find it in Firebase Console > Project Settings
FIREBASE_PROJECT_ID=""

# Optional: Path to Firebase service account key JSON file
# Download from: Firebase Console > Project Settings > Service Accounts > Generate new private key
# If not set, the scripts will try to use Application Default Credentials
# (requires: gcloud auth application-default login)
# Example: "/path/to/your/serviceAccountKey.json"
# GOOGLE_APPLICATION_CREDENTIALS=""
