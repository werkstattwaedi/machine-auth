# Development firmware application
# Includes debug features, verbose logging, test modes
#
# Usage:
#   bazel build --config=p2 //maco_firmware/apps/dev:dev_firmware
#   bazel run --config=p2 //maco_firmware/apps/dev:flash

# load("@particle_bazel//rules:particle_firmware.bzl", "particle_firmware", "particle_flash_binary")

# TODO: Add development firmware target
# particle_firmware(
#     name = "dev_firmware",
#     srcs = ["main.cc"],
#     deps = [
#         "//maco_firmware/modules/...",
#         "//maco_firmware/system",
#     ],
# )
#
# particle_flash_binary(
#     name = "flash",
#     firmware = ":dev_firmware",
# )
