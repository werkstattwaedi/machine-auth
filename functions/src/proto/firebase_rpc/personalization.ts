// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: firebase_rpc/personalization.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { KeyBytes, TagUid } from "../common";

export const protobufPackage = "maco.proto.firebase_rpc";

/** KeyDiversification RPC - computes diversified keys for tag setup */
export interface KeyDiversificationRequest {
  tokenId: TagUid | undefined;
}

export interface KeyDiversificationResponse {
  applicationKey: KeyBytes | undefined;
  authorizationKey: KeyBytes | undefined;
  reserved1Key: KeyBytes | undefined;
  reserved2Key: KeyBytes | undefined;
}

function createBaseKeyDiversificationRequest(): KeyDiversificationRequest {
  return { tokenId: undefined };
}

export const KeyDiversificationRequest: MessageFns<KeyDiversificationRequest> = {
  encode(message: KeyDiversificationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenId !== undefined) {
      TagUid.encode(message.tokenId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyDiversificationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyDiversificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tokenId = TagUid.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<KeyDiversificationRequest>): KeyDiversificationRequest {
    return KeyDiversificationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyDiversificationRequest>): KeyDiversificationRequest {
    const message = createBaseKeyDiversificationRequest();
    message.tokenId = (object.tokenId !== undefined && object.tokenId !== null)
      ? TagUid.fromPartial(object.tokenId)
      : undefined;
    return message;
  },
};

function createBaseKeyDiversificationResponse(): KeyDiversificationResponse {
  return { applicationKey: undefined, authorizationKey: undefined, reserved1Key: undefined, reserved2Key: undefined };
}

export const KeyDiversificationResponse: MessageFns<KeyDiversificationResponse> = {
  encode(message: KeyDiversificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationKey !== undefined) {
      KeyBytes.encode(message.applicationKey, writer.uint32(10).fork()).join();
    }
    if (message.authorizationKey !== undefined) {
      KeyBytes.encode(message.authorizationKey, writer.uint32(18).fork()).join();
    }
    if (message.reserved1Key !== undefined) {
      KeyBytes.encode(message.reserved1Key, writer.uint32(26).fork()).join();
    }
    if (message.reserved2Key !== undefined) {
      KeyBytes.encode(message.reserved2Key, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyDiversificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyDiversificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationKey = KeyBytes.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.authorizationKey = KeyBytes.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reserved1Key = KeyBytes.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reserved2Key = KeyBytes.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<KeyDiversificationResponse>): KeyDiversificationResponse {
    return KeyDiversificationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyDiversificationResponse>): KeyDiversificationResponse {
    const message = createBaseKeyDiversificationResponse();
    message.applicationKey = (object.applicationKey !== undefined && object.applicationKey !== null)
      ? KeyBytes.fromPartial(object.applicationKey)
      : undefined;
    message.authorizationKey = (object.authorizationKey !== undefined && object.authorizationKey !== null)
      ? KeyBytes.fromPartial(object.authorizationKey)
      : undefined;
    message.reserved1Key = (object.reserved1Key !== undefined && object.reserved1Key !== null)
      ? KeyBytes.fromPartial(object.reserved1Key)
      : undefined;
    message.reserved2Key = (object.reserved2Key !== undefined && object.reserved2Key !== null)
      ? KeyBytes.fromPartial(object.reserved2Key)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
