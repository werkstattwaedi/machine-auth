// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: firebase_rpc/session.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TagUid } from "../common";

export const protobufPackage = "maco.proto.firebase_rpc";

/** Active user session with permissions */
export interface TokenSession {
  /** NTag UUID */
  tokenId:
    | TagUid
    | undefined;
  /** Firebase session ID */
  sessionId: string;
  /** Session expiration date (unix timestamp) */
  expiration: bigint;
  /** Firebase user ID */
  userId: string;
  /** Display name for the user */
  userLabel: string;
  /** Firebase permission IDs granted for the user */
  permissions: string[];
}

/** StartSession RPC - attempts to start a session with a known tag */
export interface StartSessionRequest {
  tokenId: TagUid | undefined;
}

export interface StartSessionResponse {
  result?:
    | //
    /** Session successfully started */
    { $case: "session"; session: TokenSession }
    | //
    /** Authentication required (tag not yet authenticated) */
    { $case: "authRequired"; authRequired: AuthRequired }
    | //
    /** Session rejected (user not allowed, tag unknown, etc.) */
    { $case: "rejected"; rejected: Rejected }
    | undefined;
}

/** Tag requires 3-pass mutual authentication */
export interface AuthRequired {
}

/** Session request was rejected */
export interface Rejected {
  /** User-readable message */
  message: string;
}

/** AuthenticateNewSession RPC - initiates 3-pass mutual authentication */
export interface AuthenticateNewSessionRequest {
  tokenId:
    | TagUid
    | undefined;
  /** Challenge generated by NTag (RndB) */
  ntagChallenge: Uint8Array;
}

export interface AuthenticateNewSessionResponse {
  /** Newly generated session ID */
  sessionId: string;
  /** Combined NTag challenge response and challenge generated by cloud */
  cloudChallenge: Uint8Array;
}

/** CompleteAuthentication RPC - completes 3-pass mutual authentication */
export interface CompleteAuthenticationRequest {
  /** Session ID from AuthenticateNewSessionResponse */
  sessionId: string;
  /** NTag challenge response */
  encryptedNtagResponse: Uint8Array;
}

export interface CompleteAuthenticationResponse {
  result?:
    | //
    /** Authentication successful, session established */
    { $case: "session"; session: TokenSession }
    | //
    /** Authentication failed */
    { $case: "rejected"; rejected: Rejected }
    | undefined;
}

function createBaseTokenSession(): TokenSession {
  return { tokenId: undefined, sessionId: "", expiration: 0n, userId: "", userLabel: "", permissions: [] };
}

export const TokenSession: MessageFns<TokenSession> = {
  encode(message: TokenSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenId !== undefined) {
      TagUid.encode(message.tokenId, writer.uint32(10).fork()).join();
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.expiration !== 0n) {
      if (BigInt.asIntN(64, message.expiration) !== message.expiration) {
        throw new globalThis.Error("value provided for field message.expiration of type int64 too large");
      }
      writer.uint32(24).int64(message.expiration);
    }
    if (message.userId !== "") {
      writer.uint32(34).string(message.userId);
    }
    if (message.userLabel !== "") {
      writer.uint32(42).string(message.userLabel);
    }
    for (const v of message.permissions) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tokenId = TagUid.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiration = reader.int64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.userLabel = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.permissions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<TokenSession>): TokenSession {
    return TokenSession.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TokenSession>): TokenSession {
    const message = createBaseTokenSession();
    message.tokenId = (object.tokenId !== undefined && object.tokenId !== null)
      ? TagUid.fromPartial(object.tokenId)
      : undefined;
    message.sessionId = object.sessionId ?? "";
    message.expiration = object.expiration ?? 0n;
    message.userId = object.userId ?? "";
    message.userLabel = object.userLabel ?? "";
    message.permissions = object.permissions?.map((e) => e) || [];
    return message;
  },
};

function createBaseStartSessionRequest(): StartSessionRequest {
  return { tokenId: undefined };
}

export const StartSessionRequest: MessageFns<StartSessionRequest> = {
  encode(message: StartSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenId !== undefined) {
      TagUid.encode(message.tokenId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tokenId = TagUid.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StartSessionRequest>): StartSessionRequest {
    return StartSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartSessionRequest>): StartSessionRequest {
    const message = createBaseStartSessionRequest();
    message.tokenId = (object.tokenId !== undefined && object.tokenId !== null)
      ? TagUid.fromPartial(object.tokenId)
      : undefined;
    return message;
  },
};

function createBaseStartSessionResponse(): StartSessionResponse {
  return { result: undefined };
}

export const StartSessionResponse: MessageFns<StartSessionResponse> = {
  encode(message: StartSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.result?.$case) {
      case "session":
        TokenSession.encode(message.result.session, writer.uint32(10).fork()).join();
        break;
      case "authRequired":
        AuthRequired.encode(message.result.authRequired, writer.uint32(18).fork()).join();
        break;
      case "rejected":
        Rejected.encode(message.result.rejected, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = { $case: "session", session: TokenSession.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = { $case: "authRequired", authRequired: AuthRequired.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.result = { $case: "rejected", rejected: Rejected.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StartSessionResponse>): StartSessionResponse {
    return StartSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartSessionResponse>): StartSessionResponse {
    const message = createBaseStartSessionResponse();
    switch (object.result?.$case) {
      case "session": {
        if (object.result?.session !== undefined && object.result?.session !== null) {
          message.result = { $case: "session", session: TokenSession.fromPartial(object.result.session) };
        }
        break;
      }
      case "authRequired": {
        if (object.result?.authRequired !== undefined && object.result?.authRequired !== null) {
          message.result = {
            $case: "authRequired",
            authRequired: AuthRequired.fromPartial(object.result.authRequired),
          };
        }
        break;
      }
      case "rejected": {
        if (object.result?.rejected !== undefined && object.result?.rejected !== null) {
          message.result = { $case: "rejected", rejected: Rejected.fromPartial(object.result.rejected) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseAuthRequired(): AuthRequired {
  return {};
}

export const AuthRequired: MessageFns<AuthRequired> = {
  encode(_: AuthRequired, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthRequired {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthRequired();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AuthRequired>): AuthRequired {
    return AuthRequired.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AuthRequired>): AuthRequired {
    const message = createBaseAuthRequired();
    return message;
  },
};

function createBaseRejected(): Rejected {
  return { message: "" };
}

export const Rejected: MessageFns<Rejected> = {
  encode(message: Rejected, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rejected {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejected();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Rejected>): Rejected {
    return Rejected.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Rejected>): Rejected {
    const message = createBaseRejected();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseAuthenticateNewSessionRequest(): AuthenticateNewSessionRequest {
  return { tokenId: undefined, ntagChallenge: new Uint8Array(0) };
}

export const AuthenticateNewSessionRequest: MessageFns<AuthenticateNewSessionRequest> = {
  encode(message: AuthenticateNewSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenId !== undefined) {
      TagUid.encode(message.tokenId, writer.uint32(10).fork()).join();
    }
    if (message.ntagChallenge.length !== 0) {
      writer.uint32(18).bytes(message.ntagChallenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticateNewSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticateNewSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tokenId = TagUid.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ntagChallenge = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AuthenticateNewSessionRequest>): AuthenticateNewSessionRequest {
    return AuthenticateNewSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticateNewSessionRequest>): AuthenticateNewSessionRequest {
    const message = createBaseAuthenticateNewSessionRequest();
    message.tokenId = (object.tokenId !== undefined && object.tokenId !== null)
      ? TagUid.fromPartial(object.tokenId)
      : undefined;
    message.ntagChallenge = object.ntagChallenge ?? new Uint8Array(0);
    return message;
  },
};

function createBaseAuthenticateNewSessionResponse(): AuthenticateNewSessionResponse {
  return { sessionId: "", cloudChallenge: new Uint8Array(0) };
}

export const AuthenticateNewSessionResponse: MessageFns<AuthenticateNewSessionResponse> = {
  encode(message: AuthenticateNewSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.cloudChallenge.length !== 0) {
      writer.uint32(18).bytes(message.cloudChallenge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticateNewSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticateNewSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cloudChallenge = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AuthenticateNewSessionResponse>): AuthenticateNewSessionResponse {
    return AuthenticateNewSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticateNewSessionResponse>): AuthenticateNewSessionResponse {
    const message = createBaseAuthenticateNewSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.cloudChallenge = object.cloudChallenge ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCompleteAuthenticationRequest(): CompleteAuthenticationRequest {
  return { sessionId: "", encryptedNtagResponse: new Uint8Array(0) };
}

export const CompleteAuthenticationRequest: MessageFns<CompleteAuthenticationRequest> = {
  encode(message: CompleteAuthenticationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.encryptedNtagResponse.length !== 0) {
      writer.uint32(18).bytes(message.encryptedNtagResponse);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompleteAuthenticationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompleteAuthenticationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.encryptedNtagResponse = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<CompleteAuthenticationRequest>): CompleteAuthenticationRequest {
    return CompleteAuthenticationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CompleteAuthenticationRequest>): CompleteAuthenticationRequest {
    const message = createBaseCompleteAuthenticationRequest();
    message.sessionId = object.sessionId ?? "";
    message.encryptedNtagResponse = object.encryptedNtagResponse ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCompleteAuthenticationResponse(): CompleteAuthenticationResponse {
  return { result: undefined };
}

export const CompleteAuthenticationResponse: MessageFns<CompleteAuthenticationResponse> = {
  encode(message: CompleteAuthenticationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.result?.$case) {
      case "session":
        TokenSession.encode(message.result.session, writer.uint32(10).fork()).join();
        break;
      case "rejected":
        Rejected.encode(message.result.rejected, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompleteAuthenticationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompleteAuthenticationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = { $case: "session", session: TokenSession.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.result = { $case: "rejected", rejected: Rejected.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<CompleteAuthenticationResponse>): CompleteAuthenticationResponse {
    return CompleteAuthenticationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CompleteAuthenticationResponse>): CompleteAuthenticationResponse {
    const message = createBaseCompleteAuthenticationResponse();
    switch (object.result?.$case) {
      case "session": {
        if (object.result?.session !== undefined && object.result?.session !== null) {
          message.result = { $case: "session", session: TokenSession.fromPartial(object.result.session) };
        }
        break;
      }
      case "rejected": {
        if (object.result?.rejected !== undefined && object.result?.rejected !== null) {
          message.result = { $case: "rejected", rejected: Rejected.fromPartial(object.result.rejected) };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
