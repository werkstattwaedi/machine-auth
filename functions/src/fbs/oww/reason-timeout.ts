// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class ReasonTimeout implements flatbuffers.IUnpackableObject<ReasonTimeoutT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ReasonTimeout {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsReasonTimeout(bb:flatbuffers.ByteBuffer, obj?:ReasonTimeout):ReasonTimeout {
  return (obj || new ReasonTimeout()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsReasonTimeout(bb:flatbuffers.ByteBuffer, obj?:ReasonTimeout):ReasonTimeout {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ReasonTimeout()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static startReasonTimeout(builder:flatbuffers.Builder) {
  builder.startObject(0);
}

static endReasonTimeout(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createReasonTimeout(builder:flatbuffers.Builder):flatbuffers.Offset {
  ReasonTimeout.startReasonTimeout(builder);
  return ReasonTimeout.endReasonTimeout(builder);
}

unpack(): ReasonTimeoutT {
  return new ReasonTimeoutT();
}


unpackTo(_o: ReasonTimeoutT): void {}
}

export class ReasonTimeoutT implements flatbuffers.IGeneratedObject {
constructor(){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return ReasonTimeout.createReasonTimeout(builder);
}
}
